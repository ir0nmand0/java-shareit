logging:
  level:
    org:
      springframework:
        web:
          client:
            RestTemplate: DEBUG
server:
  port: 8080
shareit-server:
  url: http://localhost:9090
  users: "/users"
  items: "/items"
  requests: "/requests"
  bookings: "/bookings"