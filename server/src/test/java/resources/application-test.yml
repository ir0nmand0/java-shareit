spring:
    config:
        activate:
            on-profile: ci,test
    datasource:
        driver-class-name: org.h2.Driver
        password: test
        url: jdbc:h2:mem:shareit
        username: test
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.H2Dialect
    sql:
        init:
            platform: h2
